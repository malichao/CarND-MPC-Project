#include "vehicle.h"

Vehicle::Vehicle() {}

void Vehicle::Drive(const double dt)
{
//    x = x + v * cos(psi) * dt;
//    y = y + v * sin(psi) * dt;
//    psi = psi + v / Lf * delta * dt;
//    v = v + a * dt;
}
